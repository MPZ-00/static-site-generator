package staticsitegenerator

// GenerateSite generates a static site from the given source directory to the given destination directory
func GenerateSite(sourceDir string, destDir string) error {
	return nil
}
